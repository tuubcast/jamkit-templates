import themes.sbss

#img_logo: width=120dp

/catalog: page-background-color=$THEME_COLOR_SPLASH_BACKGROUND

if $ORIENTATION == "portrait"
	#img_splash: width=0.5pw, height=0.5pw, position=abs, gravity=center, y=-0.24pw
	#img_logo: position=abs, gravity=bottom, y=-(1eb+40dp)
else
	#img_splash: width=0.5ph, height=0.5ph, position=abs, gravity=center
	#img_logo: position=abs, gravity=left-top, x=20dp, y=20dp
end
